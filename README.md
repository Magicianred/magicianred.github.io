# Simone Paolucci — Professional Profile

Static, accessible professional profile for GitHub Pages.

## Local preview

Run `python -m http.server 4173` from the repository root and open
`http://localhost:4173/`.
